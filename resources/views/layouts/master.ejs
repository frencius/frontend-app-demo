<!DOCTYPE html>
<html lang="en">
<head>
	<% include ./includes/head %>

	<% if (typeof addcss != 'undefined') { %>
	<%-addcss%>
	<% } %>

	<% if (typeof headjs != 'undefined') { %>
	<%-headjs%>
	<% } %>

</head>
<body>

	<% include ./includes/header %>

	<%-content%>

	<% include ./includes/footer %>

</body>

<% if (typeof addjs != 'undefined') { %>
<%-addjs%>
<% } %>

</html>
